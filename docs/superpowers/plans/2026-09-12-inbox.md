# Inbox implementation and verification record

Approved scope: extend the existing synthetic demo with a manually checked firm mailbox, reply-to-request matching, and explicit adviser acceptance. User approved this sequence in chat after the pickup review. This record captures the implemented scope and remaining live verification.

## Completed

- [x] Add a gated, read-only Gmail IMAP reader (`SMTP_USER`, `SMTP_PASS`, `OUTREACH_ALLOWED_RECIPIENTS`, `AI_ASSIST_PASSCODE`). Fixed server, exact sender recheck, recent-30-day window, newest-50 cap, bounded text, attachment metadata only, deadline and connection cleanup.
- [x] Persist Inbox proposals in the existing browser workspace with backward-compatible defaults. Deduplicate polling, retain original retrieved text, and record per-message/request acceptances.
- [x] Match sent thread IDs and sender; require explicit manual selection for unmatched replies. Append accepted text to existing answers, reopen review and record provenance. Never automatically approve evidence.
- [x] Add an adviser Inbox tab, manual Check inbox, reply cards, request choice, editable relevant text and an Open request and AI assist action.
- [x] Hold the shared workspace lock while sending, preventing an overlapping Inbox operation from dropping the send receipt. Surface persistence failures clearly as already-sent mail.
- [x] Verify with 89 unit/route tests, four browser journeys, type checking and a production build. Independent reviewer found no material findings in the new Inbox and send-lock changes.
- [x] Update stale README/pickup status and document comparable products with primary sources.

## Live follow-through

- [x] Create/configure the dedicated firm mailbox (family address remains the external recipient). Credentials are entered directly in local/project environment settings.
- [x] Deploy Inbox changes to the existing hackathon project and verify one real outbound send.
- [x] Verify family reply → inbox match → adviser acceptance. Hosted Inbox retrieved the real family reply on 12 September 2026, matched Alex's sent outreach, and saved the relevant synthetic answer to Current-year changes with adviser review pending. A second poll returned zero new replies.
- [x] Exercise all four AI actions with the user-approved faster model. Real hosted responses and isolated acceptance checks passed on 13 September; see `docs/ai-verification.md`. K3 timeouts are historical, and live provider behavior is still variable.
- [ ] Rehearse the complete synthetic evidence/review journey for recording.

## Explicit limits

Manual polling only. No attachment bytes or PDF/OCR. No production authentication, shared server persistence, automatic cross-entity reconciliation or annual snapshot rollover. The client-view toggle remains a simulation. Per-workspace retention: 200 replies and 1,000 acceptance records. Partial or HTML-only email requires reading the original firm mailbox.
